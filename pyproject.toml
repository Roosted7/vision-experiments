[project]
name = "vision-tests"
version = "0.1.0"
description = "Object detection with RT-DETR, YOLOv9s, YOLOv10s using ONNX and camera"
requires-python = ">=3.10"

[dependency-groups]
dev = []

[tool.uv]
dev-dependencies = []
